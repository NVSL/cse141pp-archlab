Only in ./benchmarks/spec2017: benchspec
diff -x MANIFEST -ru ./benchmarks/.pristine_spec2017/bin/common/setup_common.pl ./benchmarks/spec2017/bin/common/setup_common.pl
--- ./benchmarks/.pristine_spec2017/bin/common/setup_common.pl	2018-04-19 17:53:15.000000000 +0000
+++ ./benchmarks/spec2017/bin/common/setup_common.pl	2019-07-03 21:52:59.632174100 +0000
@@ -164,9 +164,10 @@
         if (-f $path) {
             # Make it relative to $SPEC for looking up in MANIFEST
             $path =~ s#^\Q${ENV{'SPEC'}}\E[/\\]*##;
-            if ($::check_integrity && !check_files(\%::file_sums, $path)) {
-                die "\n\nPart of the tools ($module in $path) is corrupt!  Aborting...\n\n";
-            }
+	    # SS: We hacked the script, so integrity checks will fail.
+            #if ($::check_integrity && !check_files(\%::file_sums, $path)) {
+            #    die "\n\nPart of the tools ($module in $path) is corrupt!  Aborting...\n\n";
+            #}
             eval "$pre require \"$module\";";
             print '.' unless ($quiet);
             if ($@) {
diff -x MANIFEST -ru ./benchmarks/.pristine_spec2017/bin/common/util_common.pl ./benchmarks/spec2017/bin/common/util_common.pl
--- ./benchmarks/.pristine_spec2017/bin/common/util_common.pl	2018-05-22 16:14:48.000000000 +0000
+++ ./benchmarks/spec2017/bin/common/util_common.pl	2019-07-03 21:52:59.632174100 +0000
@@ -1253,6 +1253,7 @@
                 open(STDERR, '>'.$opts->{'errfile'}) or die "Can't redirect stderr to '".$opts->{'errfile'}."': $!\n";
             }
         }
+	print STDERR "EXECING: exec(".join(' ', @cmd).")\n";
         if (!exec @cmd) {
             # Command didn't exist _and_ no shell was involved
             print STDERR "exec(".join(' ', @cmd).") failed: $!\n";
diff -x MANIFEST -ru ./benchmarks/.pristine_spec2017/bin/harness/benchmark.pm ./benchmarks/spec2017/bin/harness/benchmark.pm
--- ./benchmarks/.pristine_spec2017/bin/harness/benchmark.pm	2017-05-12 15:27:56.000000000 +0000
+++ ./benchmarks/spec2017/bin/harness/benchmark.pm	2019-07-03 21:52:59.636174116 +0000
@@ -3249,6 +3249,14 @@
 
         $start = time;
         my $outname = istrue($me->fake) ? 'benchmark_run' : undef;
+	# SS: This is as close as we will get to the executables, since they don't ship the source for specinvoke, which is what actually gets called here.
+	if ($ENV{'ARCHLAB_RUNNER'}) {
+	    my $t = $ENV{'ARCHLAB_RUNNER'};
+	    my $l = $me->name.".".$me->num;
+	    $t =~ s/BENCHMARK_LABEL/$l/g;
+	    $command = "$t $command"
+	}
+	print STDERR "RUNNING BENCHMARK: $command";
         my $rc = ::log_system($command, { 'basename' => $outname, 'env_vars' => istrue($me->env_vars) });
         $stop = time;
         $elapsed = $stop-$start;
Only in ./benchmarks/spec2017/bin: lib
Only in ./benchmarks/spec2017/bin: packagename
Only in ./benchmarks/spec2017/bin: specinvoke
Only in ./benchmarks/spec2017/bin: specmake
Only in ./benchmarks/spec2017/bin: specmd5sum
Only in ./benchmarks/spec2017/bin: specperl
Only in ./benchmarks/spec2017/bin: specrxp
Only in ./benchmarks/spec2017/bin: specsha256sum
Only in ./benchmarks/spec2017/bin: specsha512sum
Only in ./benchmarks/spec2017/bin: spectar
Only in ./benchmarks/spec2017/bin: specxz
Only in ./benchmarks/spec2017: config
Only in ./benchmarks/spec2017: iso
Only in ./benchmarks/spec2017: MANIFEST.rej
Only in ./benchmarks/spec2017: result
Only in ./benchmarks/spec2017: tmp
Only in ./benchmarks/spec2017: TOOLS.sha512
